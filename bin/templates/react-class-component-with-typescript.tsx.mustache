import * as React from 'react';

interface Props {
{{#props}}
  {{name}}: {{value}};
{{/props}}
};

interface State {
{{#state}}
  {{name}}: {{value}};
{{/state}}
}

export default class {{name}} extends React.Component<Props, State> {
  render() {
    const {
    {{#props}}
      {{name}},
    {{/props}}
    } = this.props;

    return <div />
  }
};