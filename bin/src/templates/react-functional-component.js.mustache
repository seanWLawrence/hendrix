import React from 'react'
import PropTypes from 'prop-types'

const {{name}} = ({ {{#props}}{{name}}, {{/props}} }) => {
  return <div />
}

{{name}}.propTypes = {
{{#props}}
  {{name}}: PropTypes.{{value}}.isRequired,
{{/props}}
};

export default {{name}};